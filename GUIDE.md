# KANT 학습 운영 가이드

이 문서만 읽어도 강의 PDF, ChatGPT 프로젝트, GitHub TIL 저장소를 어떻게 함께 사용할지 알 수 있도록 작성했습니다.

## 1. 가장 중요한 결론

앞으로의 학습 순서는 다음과 같습니다.

```text
정리하기 전에 회상한다.
설명을 읽기 전에 무엇을 모르는지 드러낸다.
노트를 쓰기 전에 계산하거나 적용한다.
canonical note를 만들기 전에 내 말로 설명한다.
며칠 뒤 다시 설명할 수 있어야 완료로 본다.
```

강의 PDF를 강의별로 다시 길게 요약하지 않습니다. 긴 요약은 읽을 때 익숙한 느낌인 `recognized`를 만들 수 있지만, 자료 없이 꺼내는 능력이나 새 문제에 쓰는 능력은 확인하지 못합니다. 작성 시간까지 커지면 적용과 복습이 밀려 새로운 정리 부채가 됩니다.

여러 PDF가 있다면 먼저 개념 단위로 분해하고 선수지식 순서로 재배열합니다. 실제 학습은 한 번에 하나의 핵심 질문만 처리합니다.

## 2. 도구별 역할과 진로 연결

### ChatGPT 프로젝트

- 강의 PDF 원본 보관
- 전체 개념 inventory와 선수지식 그래프 작성
- 이해도 진단
- 어려운 개념 설명
- 작은 숫자 예제와 shape 추론
- 코드 및 실험 설계
- teach-back 검토

### GitHub 저장소

- 통과한 개념의 canonical note
- 직접 실행한 실험
- 그날 바뀐 이해를 적은 짧은 TIL
- 복습 기록과 진행 상태

### 강의 PDF

- 학습 범위와 예제의 원본
- 노트로 그대로 복제할 대상이 아님

### 목표 직무와 연결하는 질문

각 개념에서 모든 진로 키워드를 억지로 붙이지는 않습니다. 대신 관련이 있을 때 다음 연결을 확인합니다.

- **LLM Systems / Inference Optimization**: 연산량, 메모리, latency, throughput, batching, KV cache에 어떤 영향을 주는가?
- **Post-training**: loss, gradient, 데이터 구성, SFT·LoRA·preference optimization과 어떻게 연결되는가?
- **Evaluation**: 무엇을 측정하며, leakage·오염·분산·judge reliability 때문에 어떤 오판이 생길 수 있는가?
- **공통 기반**: 수식이 어떤 Tensor 연산이 되고 PyTorch 코드의 어느 줄에 대응하는가?

지금 개념과 직접 관련이 없다면 연결을 만들기 위해 문서를 늘리지 않습니다.

## 3. 강의 PDF를 받은 날

### 그날의 행동 순서

1. PDF 파일명, 날짜, 범위만 `curriculum/source-index.md`에 등록합니다.
2. PDF와 기존 노트를 닫고 5~10분 동안 기억나는 것과 모르는 것을 적습니다.
3. ChatGPT에 요약 대신 diagnostic questions 3~5개를 요청합니다.
4. 답을 기준으로 선수지식 부족과 이번 강의의 핵심 개념을 구분합니다.
5. 작은 숫자·shape·적용 문제 중 하나를 풀고 teach-back합니다.
6. 실제 증거가 있을 때만 TIL, concept, lab 중 필요한 파일만 만듭니다.
7. `curriculum/progress.md`에 현재 상태와 다음 복습일을 남깁니다.

첫날에 끝내야 하는 것은 강의 전체 정리가 아니라 **현재 위치와 다음 학습 행동의 확인입니다.**

### 전체 자료를 올린 뒤 curriculum map 만들기

PDF를 모두 업로드한 뒤에도 곧바로 1강부터 요약하지 않습니다. 다음 요청으로 전체 지도를 한 번 만들고, 그 뒤에는 한 개념씩 학습합니다.

다음 요청을 사용합니다.

```text
업로드한 모든 KANT 강의 PDF와 GitHub 저장소의 기존 노트를 함께 분석해줘.

개별 강의를 순서대로 요약하거나 최종 개념 문서를 대량 생성하지 마.

먼저 다음만 수행해줘.
1. 전체 개념 inventory
2. 선수지식 dependency graph
3. 여러 강의에 반복되는 중복 개념 mapping
4. LLM Systems / Post-training / Evaluation 목표 기준 P0·P1·P2 우선순위
5. 기존 노트의 중복·오분류·충돌 후보
6. 개념별 diagnostic questions
7. 강의 날짜가 아닌 선수지식 기준 학습 순서
8. curriculum/source-index.md, course-map.md, progress.md 갱신
9. 첫 번째 학습 단위 하나만 선정

아직 concepts 문서를 일괄 작성하지 마.
```

검토한 결과만 `curriculum/source-index.md`, `course-map.md`, `progress.md`에 반영합니다. PDF 전체가 아직 준비되지 않았다면 임시 추정 지도를 사실처럼 확정하지 않습니다.

## 4. 매 수업의 표준 학습 사이클

### 단계 0. 학습 단위를 하나로 제한

한 세션에서 다루는 핵심 개념은 원칙적으로 하나입니다.

좋은 단위:

- 분산이 무엇인가
- 배깅이 왜 분산을 줄이는가
- 왜 $QK^\top$가 토큰 간 점수 행렬이 되는가
- 왜 cross entropy가 language modeling loss가 되는가

너무 큰 단위:

- 랜덤포레스트 전체
- Transformer 전체
- 확률통계 전체

큰 주제는 여러 개념으로 분해합니다.

### 단계 1. Closed-book 회상

PDF와 기존 노트를 닫고 5~10분 동안 답합니다.

```text
1. 이 개념을 한 문장으로 어떻게 설명할 수 있는가?
2. 왜 필요한가?
3. 관련 수식이나 Tensor shape은 무엇인가?
4. 이전 개념과 어떻게 연결되는가?
5. 정확히 설명하기 어려운 부분은 무엇인가?
```

완성된 답을 쓰는 것이 목적이 아닙니다. 빈칸과 잘못된 가정을 드러내는 것이 목적입니다.

### 단계 2. 진단 질문

다음 요청을 사용합니다.

```text
오늘은 [개념명]을 학습한다.

바로 설명하지 말고 내 이해도를 확인하는 질문 3~5개를 먼저 제시해줘.
내 답을 받은 뒤:
1. 맞는 부분
2. 수정해야 하는 부분
3. 빠진 선수지식
을 구분해줘.
```

답을 보지 않고 설명부터 읽으면 `recognized` 상태에서 멈추기 쉽습니다.

### 단계 3. 빈틈 중심 설명

진단 후 다음 순서로 설명을 받습니다.

```text
문제가 무엇인가
→ 왜 필요한가
→ 직관
→ 작은 숫자 예제
→ 수식과 각 항의 의미
→ Tensor shape과 축의 의미
→ Python/PyTorch 코드
→ 실제 ML/LLM에서의 사용
```

이미 정확히 설명한 부분은 압축합니다. 막힌 선수지식은 먼저 보충합니다.

진단 결과에 따라 설명 범위를 다음처럼 조절합니다.

| 진단 결과 | 다음 행동 |
|---|---|
| 핵심 질문 대부분에 답하지 못함 | 선수지식 한 개로 내려가 직관과 작은 숫자부터 다시 시작 |
| 정의는 알지만 수식·shape에서 막힘 | 일반 설명은 줄이고 손계산과 축 의미에 집중 |
| 설명은 되지만 새 문제에서 막힘 | 추가 강의 대신 변형 문제, 반례, 디버깅 수행 |
| 설명과 적용이 모두 정확함 | 설명 반복을 멈추고 구현 또는 delayed review로 이동 |

질문 수의 점수만으로 기계적으로 판단하지 않습니다. 우연히 맞힌 답, 용어만 외운 답, 근거 없는 확신은 통과 증거가 아닙니다.

### 단계 4. 적용

설명을 읽은 직후 노트를 작성하지 않습니다. 다음 중 최소 하나를 수행합니다.

- 작은 숫자를 직접 계산
- Tensor shape을 자료 없이 추론
- 코드 출력 예측
- 코드 빈칸 채우기
- 잘못된 설명이나 구현 교정
- 새로운 그래프 또는 모델 결과 해석
- 라이브러리 없이 최소 구현

다음 기준으로 적용 방법을 고릅니다.

- 행렬·Attention·batch 연산처럼 축이 핵심이면 작은 숫자 계산과 shape 추론을 포함합니다.
- loss·확률·평가지표처럼 값의 의미가 핵심이면 손계산 후 결과를 해석합니다.
- 라이브러리 동작, gradient, numerical behavior가 핵심이면 출력 예측 후 최소 코드를 실제 실행합니다.
- 코드가 개념 이해를 더 증명하지 못한다면 lab을 억지로 만들지 않습니다.

예시:

```text
Q: (3, 2)
K: (3, 2)

1. K^T의 shape은?
2. QK^T의 shape은?
3. 결과의 i행 j열은 무엇을 뜻하는가?
4. 행별 softmax를 적용하는 이유는?
```

### 단계 5. Teach-back

자료를 닫고 3~7문장으로 다시 설명합니다.

```text
내 설명:
[내 말로 작성]

이 설명에서:
- 맞는 부분
- 애매한 부분
- 개념적으로 틀린 부분
- 빠진 핵심
을 구분해서 교정해줘.
```

정확한 정의만 반복하는 것으로는 부족합니다. 무엇을 해결하는지, 왜 그 연산을 쓰는지, 작은 예제 또는 shape과 어떻게 연결되는지를 자신의 말로 설명해야 `explained`로 올릴 수 있습니다.

### 단계 6. 기록 여부 결정

다음 표에 따라 파일을 만듭니다.

| 상황 | 기록 위치 | 행동 |
|---|---|---|
| 읽었지만 아직 설명 못 함 | `curriculum/progress.md` | `seen` 또는 `recognized`만 표시 |
| 오개념 하나가 수정됨 | `til/YYYY-MM/YYYY-MM-DD.md` | frontmatter를 제외한 핵심 내용 5~15줄 기록 |
| 하나의 개념을 설명·적용함 | `concepts/<domain>/<slug>.md` | canonical note 생성 또는 갱신 |
| 코드가 이해의 핵심이고 실제 실행함 | `labs/<domain>/<concept>/` | 명령, 코드, 실제 출력, 해석 저장 |
| 며칠 뒤 재검증함 | `reviews/YYYY-Www.md` | 성공·실패와 상태 변경 |
| 이해 변화나 새 증거가 없음 | 없음 | 파일을 만들지 않고 다음 학습 행동만 정함 |

이해하지 못한 내용을 노트 형식으로 예쁘게 만들지 않습니다.

### 단계 7. 간격 반복

핵심 개념은 다음 간격으로 다시 확인합니다.

- 1일 뒤: 핵심 정의와 이유
- 3일 뒤: 작은 숫자 또는 shape 문제
- 7일 뒤: 새로운 응용 문제
- 14일 뒤: 관련 개념과 연결해서 설명

14일 뒤에도 자료 없이 설명하고 적용하면 `retained` 후보입니다.

## 5. 파일을 어디에 써야 하는가

### `concepts/`: 단일 기준 문서

같은 개념을 여러 강의에서 다시 배워도 새 문서를 만들지 않습니다.

예:

```text
concepts/ml/bias-variance.md
```

이후 랜덤포레스트, 정규화, 학습곡선 강의에서 편향-분산이 다시 등장하면 이 문서를 갱신하고 각 TIL에서는 링크만 남깁니다.

canonical note에는 최소한 다음이 있어야 합니다.

- 해결하려는 문제
- 직관
- 작은 숫자 예제
- 정확한 정의 또는 수식
- shape이 있는 경우 축의 의미
- 코드 또는 lab 링크(코드가 이해에 필요한 경우)
- ML/LLM 연결(직접 관련이 있는 경우)
- 확인된 오개념 또는 헷갈리기 쉬운 부분(실제로 있을 때)
- 자료 없이 설명한 최종 요약

생성 기준은 다음 세 가지를 모두 만족하는 것입니다.

1. 기존 `concepts/`에 같은 개념이 없는지 검색했다.
2. 자료 없이 핵심을 설명해 `explained` 증거가 있다.
3. 처음 보는 숫자·shape·해석 문제를 풀어 `applied` 증거가 있다.

코드 동작 자체가 개념의 핵심이면 `implemented`까지 확인합니다. 조건을 통과하지 못하면 완성된 concept note를 먼저 만들지 않습니다.

### `til/`: 변경 로그

TIL은 강의노트가 아닙니다.

좋은 TIL:

```text
이전에는 작은 train-validation gap이면 좋은 모델이라고 생각했다.
하지만 두 점수가 모두 낮으면 고편향 후보일 수 있다.
새 학습곡선 세 개를 보고 원인 후보와 다음 실험을 구분했다.
남은 질문은 편향-분산 분해의 기대값 전개다.
```

나쁜 TIL:

```text
강의 슬라이드의 모든 항목을 순서대로 다시 설명한 800줄 문서
```

TIL은 매일 의무적으로 만들지 않습니다. 이전 생각, 수정된 이해, 그 수정이 맞다는 증거를 구체적으로 쓸 수 있는 날만 작성합니다. 단순 출석, PDF 읽기, ChatGPT 설명 복사는 생성 사유가 아닙니다.

### `labs/`: 실행 증거

코드가 도움이 되는 개념은 문서 설명보다 실험을 우선합니다.

- 수식의 어느 항이 어느 코드인지 표시
- 입력 및 출력 shape 기록
- 실제 실행 결과만 기록
- 예상과 결과가 달랐다면 원인 분석
- 관련 canonical note 링크

실행 명령과 환경 가정이 있어 다른 날 다시 돌릴 수 있어야 합니다. 예상 출력만 있거나 코드 조각을 실행하지 않았다면 lab으로 저장하지 않습니다.

### `reviews/`: 유지 여부 확인

주간 복습에서 다음을 기록합니다.

- 자료 없이 설명에 성공한 개념
- 다시 막힌 개념
- 상태를 올리거나 내린 이유
- 다음 주에 재학습할 prerequisite

### 아무 문서도 만들지 않는 경우

다음 상황에서는 파일을 만들지 않는 것이 정상입니다.

- 설명을 읽을 때만 이해되고 자료 없이 설명하지 못함
- 기존 concept 또는 lab이 이미 충분하고 새 증거가 없음
- 코드를 실행하지 않았고 예상 결과만 있음
- 강의를 들었지만 오개념 수정, 적용, 구현, 복습 결과가 없음
- 피곤하거나 시간이 부족해 진단만 했으며 다음 행동만 정함

필요하면 `curriculum/progress.md`의 상태와 다음 행동만 갱신합니다. 템플릿의 빈칸을 채우기 위한 문서는 만들지 않으며, 템플릿의 해당 없는 선택 섹션은 삭제합니다.

## 6. 기록 분량 제한과 종료 규칙

정리 부채를 다시 만들지 않기 위한 기본 제한입니다.

| 산출물 | 기본 분량 |
|---|---:|
| 일일 TIL | frontmatter를 제외한 핵심 내용 5~15줄 |
| closed-book 회상 | 5~10분 |
| canonical note | 핵심 질문 하나 |
| 한 세션의 신규 개념 | 원칙적으로 1개 |
| 실험 | 한 가설을 검증하는 최소 코드 |
| 주간 리뷰 | 15~30분 |

분량이 필요 이상으로 커지면 문서화를 중단하고 개념을 더 잘게 나눕니다.

## 7. 기존 긴 TIL 처리

기존 `content/` 문서는 삭제하지 않습니다. 또한 과거 문서를 전부 다시 고치지 않습니다. 현재 중복, 오분류, 장문, placeholder 후보는 [curriculum/note-inventory.md](./curriculum/note-inventory.md)에 기록되어 있습니다.

### 마이그레이션이 필요한 시점

다음 조건 중 하나가 생겼을 때만 기존 노트를 꺼냅니다.

- 같은 개념이 새 강의에서 다시 등장함
- 현재 학습 단위의 선수지식으로 필요함
- 기존 문서끼리 내용이 충돌함
- 공개 가능한 canonical note가 필요함

### 마이그레이션 절차

```text
기존 관련 노트 검색
→ 유효한 설명·예제만 추출
→ 직접 다시 설명하고 검증
→ concepts의 단일 문서에 통합
→ 필요하면 lab 추가
→ progress 상태 갱신
→ 기존 파일은 원본 기록으로 보존
```

기존 글이 길다는 이유만으로 `applied`나 `retained` 상태로 간주하지 않습니다.

한 번의 migration 범위는 **개념 하나입니다.** 먼저 canonical note를 안정시키고 기존 링크를 확인한 뒤에도 원본 파일 이동은 별도 작업으로 남깁니다. 이렇게 해야 과거 노트를 전부 다시 쓰는 두 번째 backlog가 생기지 않습니다.

## 8. 그대로 복사해 쓰는 ChatGPT 요청 예시

특정 실행 모드나 저장소 전용 학습 Skill은 필요하지 않습니다. 현재 대화에서 아래 요청을 그대로 사용하고, 답변을 읽기 전에 먼저 자신의 답을 제출합니다.

### 전체 PDF curriculum map

```text
업로드한 전체 PDF를 강의 순서가 아니라 개념과 선수지식 기준으로 재구성해줘.
중복 개념, 빠진 prerequisite, P0·P1·P2 우선순위,
LLM Systems / Inference Optimization / Post-training / Evaluation 연결을 보여줘.
아직 concepts 문서를 만들지 말고 curriculum 변경안과 첫 학습 단위 하나만 제안해줘.
```

### 설명 전 진단

```text
오늘 [개념]을 학습하려고 한다.
바로 설명하지 말고 prerequisite, 직관, 작은 계산,
수식·Tensor shape, 실제 적용을 확인하는 질문 3~5개를 먼저 줘.
내 답을 받은 뒤 맞는 부분, 틀린 부분, 애매한 표현, 빠진 선수지식을 구분해줘.
```

### 빈틈만 설명하고 적용하기

```text
내 진단 답변에서 부족했던 [부분]만 설명해줘.
문제 → 필요성 → 직관 → 작은 숫자 예제 → 수식의 각 항
→ Tensor shape와 축 의미 → 최소 Python/PyTorch 코드
→ 실제 ML/LLM 사용 순서로 연결해줘.
이미 맞힌 기초 내용은 압축하고 마지막에 새로운 적용 문제 하나를 줘.
```

### Teach-back 교정

```text
아래는 자료를 닫고 쓴 내 설명이다.
[내 설명]

맞는 부분, 사소한 표현 문제, 개념적 오류, 빠진 핵심을 구분해줘.
정답 설명을 통째로 다시 쓰기 전에 내가 고칠 수 있는 질문을 먼저 줘.
수정 후 explained/applied 기준 중 무엇을 통과했는지도 근거와 함께 판단해줘.
```

### 기존 노트 하나를 점진 통합

```text
[개념]과 관련된 content 노트를 찾아 중복, 충돌, 유용한 예제 후보를 정리해줘.
원본은 이동하거나 수정하지 마.
내가 closed-book 설명과 새 적용 문제를 통과한 뒤에만
기존 concepts 문서 갱신 또는 canonical note 하나의 생성안을 제시해줘.
```

## 9. 하루 85분 운영 예시

| 경과 시간 | 행동 | 산출물 |
|---|---|---|
| 0~10분 | 자료를 닫고 회상 | 답하지 못한 질문 목록 |
| 10~25분 | diagnostic questions와 답변 교정 | 선수지식 또는 핵심 빈틈 하나 |
| 25~45분 | 빈틈 설명과 작은 숫자 예제 | 손계산 또는 정확한 직관 |
| 45~65분 | shape·새 적용 문제·최소 코드 | 적용 또는 실행 증거 |
| 65~75분 | 자료 없이 teach-back 후 수정 | 3~7문장 설명 |
| 75~85분 | 최소 기록과 복습 예약 | progress, 필요할 때만 TIL/concept/lab |

한 세션에 신규 핵심 개념 하나면 충분합니다. 코드 실험이 길어지면 문서화를 생략하고 다음 세션에 이어갑니다. 마지막 10분에 남길 증거가 없다면 억지로 TIL을 쓰지 않고 실패 지점과 다음 문제만 progress에 남깁니다.

## 10. 이해 상태와 학습 완료 기준

| 상태 | 판정 기준 | 아직 부족한 것 |
|---|---|---|
| `seen` | 강의나 노트에서 접함 | 이해 여부를 아직 진단하지 않음 |
| `recognized` | 설명을 보며 따라갈 수 있음 | 자료 없이 독립 설명하지 못함 |
| `explained` | 자료 없이 문제·이유·원리를 정확히 설명 | 새 상황에서의 적용 증거 |
| `applied` | 처음 보는 숫자·shape·해석 문제를 해결 | 구현이 핵심인 경우 실행 증거 |
| `implemented` | 최소 코드로 구현하거나 오류를 찾아 수정 | 시간이 지난 뒤의 재현 |
| `retained` | 7~14일 뒤 다시 설명하고 적용 | 이후에도 필요한 주기로 유지 |

`recognized`와 `explained`의 차이는 “보면 안다”와 “자료 없이 꺼낼 수 있다”입니다. `explained`와 `applied`의 차이는 “말할 수 있다”와 “새 문제에 쓸 수 있다”입니다. `implemented`는 라이브러리 호출을 복사한 것이 아니라, 입력·출력 shape과 코드가 수식의 어느 부분인지 설명하며 실행한 상태입니다.

핵심 개념은 다음 질문에 답할 수 있어야 합니다.

1. 이 개념이 해결하는 문제는 무엇인가?
2. 왜 이 연산 또는 정의가 필요한가?
3. 작은 숫자로 직접 계산할 수 있는가?
4. 수식의 각 항이 무엇을 의미하는가?
5. Tensor shape이 왜 그렇게 되는가?
6. 코드의 각 줄을 수식과 연결할 수 있는가?
7. 새로운 사례에 적용할 수 있는가?
8. 며칠 뒤에도 자료 없이 설명할 수 있는가?

모든 개념에 같은 종료선을 강요하지 않습니다.

- 수학적 선수지식: 보통 `applied` 후 7~14일 복습 통과
- 구현 중심 주제: `implemented` 후 delayed review 통과
- 현재 조회만 하면 되는 P2 세부사항: `recognized`에서 멈춰도 됨

핵심 P0·P1 개념의 학습 완료는 필요한 적용 또는 구현을 마치고, 정해진 복습일에 다시 성공해 `retained`가 된 상태입니다. 첫날의 “이해한 느낌”을 완료로 기록하지 않습니다.

## 11. 막히거나 밀렸을 때

TIL이 밀렸다면 과거 날짜를 채우지 않습니다.

```text
1. 밀린 문서 작성 중단
2. progress에 seen만 표시
3. 가장 중요한 prerequisite 하나 선택
4. 진단 → 적용 → teach-back 수행
5. 오늘 바뀐 이해만 본문 5~15줄 기록
```

전체 강의 진도를 따라잡는 것보다 다음 개념을 이해할 수 있는 선수지식을 복구하는 것이 우선입니다.

backlog가 여러 강의로 늘었을 때는 30분 안에 목록만 만듭니다.

1. 각 강의를 `미진단`, `선수지식 필요`, `적용 필요`, `복습 필요` 중 하나로 표시합니다.
2. 목표 직무와 다음 강의를 동시에 막는 P0 prerequisite 하나를 고릅니다.
3. 나머지는 요약하지 않고 `curriculum/progress.md`의 대기열에 둡니다.
4. 선택한 하나에만 85분 사이클을 수행합니다.
5. 끝내지 못하면 새 문서를 만들지 않고 다음 시작점을 한 문장으로 남깁니다.

## 12. 1·3·7·14일 복습과 주간 점검

복습에서도 자료를 먼저 열지 않습니다.

| 시점 | 자료 없이 할 일 | 실패했을 때 |
|---|---|---|
| 1일 | 정의, 필요성, 핵심 수식 또는 shape 설명 | 해당 빈틈만 5분 재학습 후 다시 설명 |
| 3일 | 숫자나 shape를 바꾼 새 문제 해결 | `recognized` 또는 `explained`로 상태를 낮추고 prerequisite 확인 |
| 7일 | 반례·디버깅·응용 문제 해결 | concept 문서 확장 대신 새 적용 문제 예약 |
| 14일 | 다른 개념 및 LLM 목표와 연결해 설명·적용 | 성공 시 `retained`, 실패 시 다음 간격을 짧게 재설정 |

복습 성공은 예전 답을 외워 재현하는 것이 아니라 값, shape, 사례가 바뀌어도 원리를 사용하는 것입니다.

매주 다음을 점검합니다.

- 이번 주에 자료 없이 설명할 수 있게 된 개념은 무엇인가?
- 읽을 때만 이해되는 개념은 무엇인가?
- 코드로 확인하지 않아 불안한 개념은 무엇인가?
- 여러 노트에 중복된 개념은 무엇인가?
- 다음 주 학습을 막는 prerequisite는 무엇인가?
- LLM 시스템 또는 평가와 연결해서 설명할 수 있는가?

결과는 `reviews/`에 짧게 기록하고 `curriculum/progress.md`를 갱신합니다.

## 13. 하지 말아야 할 행동

- PDF를 받자마자 강의 순서대로 긴 요약부터 작성하지 않습니다.
- ChatGPT의 설명을 그대로 복사해 자신의 이해 증거로 기록하지 않습니다.
- 매일 커밋하기 위해 TIL을 소급 생성하지 않습니다.
- 하나의 긴 문서에 여러 canonical concept를 계속 덧붙이지 않습니다.
- 설명·적용을 통과하지 않은 내용을 매끄러운 concept note로 만들지 않습니다.
- 실행하지 않은 코드의 출력이나 실험 결과를 추정해 기록하지 않습니다.
- 기존 `content/` 문서, 이미지, 질문, 계산, 혼동 기록을 일괄 삭제·이동·재작성하지 않습니다.
- 오분류 후보를 링크 조사 없이 바로 옮기지 않습니다.
- 템플릿의 모든 섹션을 채우기 위해 근거 없는 내용이나 빈 문장을 만들지 않습니다.
- 진도 지연을 문서 작성량으로 만회하려 하지 않습니다.

막혔을 때의 기본 행동은 “더 길게 정리하기”가 아니라 **학습 단위를 줄이고, 선수지식 하나를 진단하고, 새 문제 하나를 푸는 것입니다.**
