# KANT 학습 운영 가이드

이 문서만 읽어도 강의 PDF, ChatGPT 프로젝트, GitHub TIL 저장소를 어떻게 함께 사용할지 알 수 있도록 작성했습니다.

## 1. 가장 중요한 결론

앞으로의 학습 순서는 다음과 같습니다.

```text
정리하기 전에 회상한다.
설명을 읽기 전에 무엇을 모르는지 드러낸다.
노트를 쓰기 전에 계산하거나 적용한다.
canonical note를 만들기 전에 내 말로 설명한다.
며칠 뒤 다시 설명할 수 있어야 완료로 본다.
```

강의 PDF를 강의별로 다시 길게 요약하지 않습니다. 모든 PDF를 먼저 개념 단위로 분해하고, 선수지식 순서로 재배열한 뒤, 한 번에 하나의 학습 단위만 처리합니다.

## 2. 도구별 역할

### ChatGPT 프로젝트

- 강의 PDF 원본 보관
- 전체 개념 inventory와 선수지식 그래프 작성
- 이해도 진단
- 어려운 개념 설명
- 작은 숫자 예제와 shape 추론
- 코드 및 실험 설계
- teach-back 검토

### GitHub 저장소

- 통과한 개념의 canonical note
- 직접 실행한 실험
- 그날 바뀐 이해를 적은 짧은 TIL
- 복습 기록과 진행 상태

### 강의 PDF

- 학습 범위와 예제의 원본
- 노트로 그대로 복제할 대상이 아님

## 3. 전체 PDF를 올린 직후 한 번만 할 일

PDF를 모두 업로드한 뒤 곧바로 1강부터 요약하지 않습니다. 먼저 `$kant-learning-cycle`을 사용해 전체 지도를 만듭니다.

다음 요청을 사용합니다.

```text
$kant-learning-cycle을 사용해 업로드한 모든 KANT 강의 PDF와
GitHub 저장소의 기존 노트를 함께 분석해줘.

개별 강의를 순서대로 요약하거나 최종 개념 문서를 대량 생성하지 마.

먼저 다음만 수행해줘.
1. 전체 개념 inventory
2. 선수지식 dependency graph
3. 여러 강의에 반복되는 중복 개념 mapping
4. LLM Systems / Post-training / Evaluation 목표 기준 P0·P1·P2 우선순위
5. 기존 노트의 중복·오분류·충돌 후보
6. 개념별 diagnostic questions
7. 강의 날짜가 아닌 선수지식 기준 학습 순서
8. curriculum/source-index.md, course-map.md, progress.md 갱신
9. 첫 번째 학습 단위 하나만 선정

아직 concepts 문서를 일괄 작성하지 마.
```

이 작업의 결과는 `curriculum/`에 반영합니다.

## 4. 매 수업의 표준 학습 사이클

### 단계 0. 학습 단위를 하나로 제한

한 세션에서 다루는 핵심 개념은 원칙적으로 하나입니다.

좋은 단위:

- 분산이 무엇인가
- 배깅이 왜 분산을 줄이는가
- 왜 $QK^\top$가 토큰 간 점수 행렬이 되는가
- 왜 cross entropy가 language modeling loss가 되는가

너무 큰 단위:

- 랜덤포레스트 전체
- Transformer 전체
- 확률통계 전체

큰 주제는 여러 개념으로 분해합니다.

### 단계 1. Closed-book 회상

PDF와 기존 노트를 닫고 5~10분 동안 답합니다.

```text
1. 이 개념을 한 문장으로 어떻게 설명할 수 있는가?
2. 왜 필요한가?
3. 관련 수식이나 Tensor shape은 무엇인가?
4. 이전 개념과 어떻게 연결되는가?
5. 정확히 설명하기 어려운 부분은 무엇인가?
```

완성된 답을 쓰는 것이 목적이 아닙니다. 빈칸과 잘못된 가정을 드러내는 것이 목적입니다.

### 단계 2. 진단 질문

다음 요청을 사용합니다.

```text
$kant-learning-cycle을 사용해 오늘은 [개념명]을 학습한다.

바로 설명하지 말고 내 이해도를 확인하는 질문 3~5개를 먼저 제시해줘.
내 답을 받은 뒤:
1. 맞는 부분
2. 수정해야 하는 부분
3. 빠진 선수지식
을 구분해줘.
```

답을 보지 않고 설명부터 읽으면 `recognized` 상태에서 멈추기 쉽습니다.

### 단계 3. 빈틈 중심 설명

진단 후 다음 순서로 설명을 받습니다.

```text
문제가 무엇인가
→ 왜 필요한가
→ 직관
→ 작은 숫자 예제
→ 수식과 각 항의 의미
→ Tensor shape과 축의 의미
→ Python/PyTorch 코드
→ 실제 ML/LLM에서의 사용
```

이미 정확히 설명한 부분은 압축합니다. 막힌 선수지식은 먼저 보충합니다.

### 단계 4. 적용

설명을 읽은 직후 노트를 작성하지 않습니다. 다음 중 최소 하나를 수행합니다.

- 작은 숫자를 직접 계산
- Tensor shape을 자료 없이 추론
- 코드 출력 예측
- 코드 빈칸 채우기
- 잘못된 설명이나 구현 교정
- 새로운 그래프 또는 모델 결과 해석
- 라이브러리 없이 최소 구현

예시:

```text
Q: (3, 2)
K: (3, 2)

1. K^T의 shape은?
2. QK^T의 shape은?
3. 결과의 i행 j열은 무엇을 뜻하는가?
4. 행별 softmax를 적용하는 이유는?
```

### 단계 5. Teach-back

자료를 닫고 3~7문장으로 다시 설명합니다.

```text
내 설명:
[내 말로 작성]

이 설명에서:
- 맞는 부분
- 애매한 부분
- 개념적으로 틀린 부분
- 빠진 핵심
을 구분해서 교정해줘.
```

이 단계가 통과되어야 `explained`로 올릴 수 있습니다.

### 단계 6. 기록 여부 결정

다음 표에 따라 파일을 만듭니다.

| 상황 | 기록 위치 | 행동 |
|---|---|---|
| 읽었지만 아직 설명 못 함 | `curriculum/progress.md` | `seen` 또는 `recognized`만 표시 |
| 오개념 하나가 수정됨 | `til/YYYY-MM/YYYY-MM-DD.md` | 5~15줄 기록 |
| 하나의 개념을 설명·적용함 | `concepts/<domain>/<slug>.md` | canonical note 생성 또는 갱신 |
| 코드를 실제 실행함 | `labs/<domain>/<concept>/` | 코드, 출력, 해석 저장 |
| 며칠 뒤 재검증함 | `reviews/YYYY-Www.md` | 성공·실패와 상태 변경 |

이해하지 못한 내용을 노트 형식으로 예쁘게 만들지 않습니다.

### 단계 7. 간격 반복

핵심 개념은 다음 간격으로 다시 확인합니다.

- 1일 뒤: 핵심 정의와 이유
- 3일 뒤: 작은 숫자 또는 shape 문제
- 7일 뒤: 새로운 응용 문제
- 14일 뒤: 관련 개념과 연결해서 설명

14일 뒤에도 자료 없이 설명하고 적용하면 `retained` 후보입니다.

## 5. 파일을 어디에 써야 하는가

### `concepts/`: 단일 기준 문서

같은 개념을 여러 강의에서 다시 배워도 새 문서를 만들지 않습니다.

예:

```text
concepts/ml/bias-variance.md
```

이후 랜덤포레스트, 정규화, 학습곡선 강의에서 편향-분산이 다시 등장하면 이 문서를 갱신하고 각 TIL에서는 링크만 남깁니다.

canonical note에는 최소한 다음이 있어야 합니다.

- 해결하려는 문제
- 직관
- 작은 숫자 예제
- 정확한 정의 또는 수식
- shape이 있는 경우 축의 의미
- 코드 또는 lab 링크
- ML/LLM 연결
- 헷갈리기 쉬운 부분
- 자료 없이 설명한 최종 요약

### `til/`: 변경 로그

TIL은 강의노트가 아닙니다.

좋은 TIL:

```text
이전에는 작은 train-validation gap이면 좋은 모델이라고 생각했다.
하지만 두 점수가 모두 낮으면 고편향 후보일 수 있다.
새 학습곡선 세 개를 보고 원인 후보와 다음 실험을 구분했다.
남은 질문은 편향-분산 분해의 기대값 전개다.
```

나쁜 TIL:

```text
강의 슬라이드의 모든 항목을 순서대로 다시 설명한 800줄 문서
```

TIL은 매일 의무적으로 만들지 않습니다. 이해가 실제로 바뀐 날만 작성합니다.

### `labs/`: 실행 증거

코드가 도움이 되는 개념은 문서 설명보다 실험을 우선합니다.

- 수식의 어느 항이 어느 코드인지 표시
- 입력 및 출력 shape 기록
- 실제 실행 결과만 기록
- 예상과 결과가 달랐다면 원인 분석
- 관련 canonical note 링크

### `reviews/`: 유지 여부 확인

주간 복습에서 다음을 기록합니다.

- 자료 없이 설명에 성공한 개념
- 다시 막힌 개념
- 상태를 올리거나 내린 이유
- 다음 주에 재학습할 prerequisite

## 6. 기록 분량 제한

정리 부채를 다시 만들지 않기 위한 기본 제한입니다.

| 산출물 | 기본 분량 |
|---|---:|
| 일일 TIL | 5~15줄 |
| closed-book 회상 | 5~10분 |
| canonical note | 핵심 질문 하나 |
| 한 세션의 신규 개념 | 원칙적으로 1개 |
| 실험 | 한 가설을 검증하는 최소 코드 |
| 주간 리뷰 | 15~30분 |

분량이 필요 이상으로 커지면 문서화를 중단하고 개념을 더 잘게 나눕니다.

## 7. 기존 긴 TIL 처리

기존 `content/` 문서는 삭제하지 않습니다. 또한 과거 문서를 전부 다시 고치지 않습니다.

### 마이그레이션이 필요한 시점

다음 조건 중 하나가 생겼을 때만 기존 노트를 꺼냅니다.

- 같은 개념이 새 강의에서 다시 등장함
- 현재 학습 단위의 선수지식으로 필요함
- 기존 문서끼리 내용이 충돌함
- 공개 가능한 canonical note가 필요함

### 마이그레이션 절차

```text
기존 관련 노트 검색
→ 유효한 설명·예제만 추출
→ 직접 다시 설명하고 검증
→ concepts의 단일 문서에 통합
→ 필요하면 lab 추가
→ progress 상태 갱신
→ 기존 파일은 원본 기록으로 보존
```

기존 글이 길다는 이유만으로 `applied`나 `retained` 상태로 간주하지 않습니다.

## 8. Work Mode 사용법

저장소 전용 모드는 다음과 같이 호출합니다.

```text
$kant-learning-cycle
```

권장 실행 설정을 선택할 수 있는 환경에서는 **GPT-5.6 SOL / Ultra**를 사용합니다. 저장소에는 모델값을 하드코딩하지 않고, 실행할 때 선택할 권장값으로만 관리합니다.

### 전체 PDF 재구성

```text
$kant-learning-cycle을 사용해 전체 PDF를 개념과 선수지식 기준으로 재구성해줘.
아직 개념 문서를 대량 작성하지 말고 curriculum만 갱신해줘.
```

### 오늘의 개념 학습

```text
$kant-learning-cycle을 사용해 [개념]을 학습하자.
설명 전에 진단하고, 적용 문제와 teach-back까지 진행해줘.
통과한 뒤에만 필요한 저장소 파일을 갱신해줘.
```

### 기존 노트 통합

```text
$kant-learning-cycle을 사용해 [개념]과 관련된 기존 content 노트를 찾아줘.
중복과 충돌을 분석한 뒤 내가 설명·적용을 통과하면
하나의 canonical note로 통합해줘.
```

### 코드 실험

```text
$kant-learning-cycle을 사용해 [수식/개념]을 확인하는 최소 PyTorch 실험을 설계해줘.
실행 전 예상 shape과 값을 먼저 내가 답하게 하고,
실행 결과를 labs에 남겨줘.
```

## 9. 평일 저녁 적용 예시

현재 학습 시간에는 다음 흐름이 적절합니다.

| 시간 | 행동 |
|---|---|
| 18:35~18:55 | 자료 없이 KANT 수업 핵심 회상 |
| 18:55~19:25 | 진단과 빈틈 중심 설명 |
| 19:25~20:00 | 계산, shape 문제, 코드 실험 |
| 20:00~20:15 | teach-back과 교정 |
| 20:15~20:20 | TIL 또는 progress 최소 갱신 |

문서 작성이 20분 이상 길어지면 원칙적으로 중단합니다. 그 시간은 적용 문제나 코드에 사용합니다.

## 10. 이해 완료 기준

핵심 개념은 다음 질문에 답할 수 있어야 합니다.

1. 이 개념이 해결하는 문제는 무엇인가?
2. 왜 이 연산 또는 정의가 필요한가?
3. 작은 숫자로 직접 계산할 수 있는가?
4. 수식의 각 항이 무엇을 의미하는가?
5. Tensor shape이 왜 그렇게 되는가?
6. 코드의 각 줄을 수식과 연결할 수 있는가?
7. 새로운 사례에 적용할 수 있는가?
8. 며칠 뒤에도 자료 없이 설명할 수 있는가?

모두를 한 번에 만족할 필요는 없습니다. 다만 `seen`을 `understood`로 부르지 않습니다.

## 11. 막히거나 밀렸을 때

TIL이 밀렸다면 과거 날짜를 채우지 않습니다.

```text
1. 밀린 문서 작성 중단
2. progress에 seen만 표시
3. 가장 중요한 prerequisite 하나 선택
4. 진단 → 적용 → teach-back 수행
5. 오늘 바뀐 이해만 5~15줄 기록
```

전체 강의 진도를 따라잡는 것보다 다음 개념을 이해할 수 있는 선수지식을 복구하는 것이 우선입니다.

## 12. 매주 점검할 질문

- 이번 주에 자료 없이 설명할 수 있게 된 개념은 무엇인가?
- 읽을 때만 이해되는 개념은 무엇인가?
- 코드로 확인하지 않아 불안한 개념은 무엇인가?
- 여러 노트에 중복된 개념은 무엇인가?
- 다음 주 학습을 막는 prerequisite는 무엇인가?
- LLM 시스템 또는 평가와 연결해서 설명할 수 있는가?

결과는 `reviews/`에 짧게 기록하고 `curriculum/progress.md`를 갱신합니다.
